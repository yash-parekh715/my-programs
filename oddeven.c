#include<stdio.h>
#include<conio.h>  

void main(){ 
	int a;
 	printf("enter the number"); 
	scanf("%d", &a); 
    if(a%2) { 
        printf("the entered number is odd"); 
} 
    else    { 
        printf("the entered number is even"); 
} 
getch(); 
}
